# todo: add platform / compiler selection here..

module_search_path '.'

case get_host_platform()
when :win32
	puts "Using MSVC 10 C Compiler"
	import 'msvc/msvc10.lace'
when :linux
	puts "Using clang C Compiler"
	import 'clang/clang.lace'
else
end

=begin

Supported Compiler options:
Activate with set_compiler_option( :C, <option-name>, <option-value> )

=end

import 'c_unity/c_unity.lace'

# activate unity builds:
@project.global_context.add_auto_tags( 'UNITY', tag( '.c' ) )

